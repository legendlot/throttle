{
  "name": "manifestops-worker",
  "version": "1.0.0",
  "description": "Manifest — LOT ↔ Solve Factory China-import operating system API (Cloudflare Worker)",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "type": "commonjs",
  "devDependencies": {
    "wrangler": "^4.83.0"
  }
}

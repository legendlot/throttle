'use client';
// Stub — replaced in Task 15 with the real per-person adherence oversight panel.
export function OversightPanel() { return <div className="cl-empty">Loading…</div>; }
export default OversightPanel;

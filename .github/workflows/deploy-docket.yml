name: Deploy Docket

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout monorepo
        uses: actions/checkout@v4

      - name: Setup Node 20
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Build Docket
        run: npx turbo build --filter=@throttle/docket
        env:
          NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
          NEXT_PUBLIC_SUPABASE_ANON_KEY: ${{ secrets.NEXT_PUBLIC_SUPABASE_ANON_KEY }}
          NEXT_PUBLIC_WORKER_URL: https://docketops.afshaan.workers.dev
          NEXT_PUBLIC_DOCKETOPS_URL: https://docketops.afshaan.workers.dev

      - name: Deploy to legendlot/docket
        uses: peaceiris/actions-gh-pages@v3
        with:
          personal_token: ${{ secrets.DEPLOY_TOKEN }}
          external_repository: legendlot/docket
          publish_branch: gh-pages
          publish_dir: apps/docket/out
          cname: docket.legendoftoys.com
          commit_message: 'deploy: Docket org task manager (LOT Monorepo)'
          force_orphan: true

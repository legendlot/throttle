{
  "name": "odoops-worker",
  "version": "1.0.0",
  "description": "Odo — LOT consolidated cross-channel sales dashboard API (Cloudflare Worker)",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "test": "node --test 'src/**/*.test.mjs'"
  },
  "type": "commonjs",
  "devDependencies": {
    "wrangler": "^4.83.0"
  }
}

{
  "name": "throttle-monorepo",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint:ci": "eslint --no-eslintrc -c ./.eslintrc.ci.json --ext .js apps/*/src packages/*/"
  },
  "devDependencies": {
    "turbo": "^1.13.4"
  }
}
